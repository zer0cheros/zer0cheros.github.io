const menuClick = document.querySelector('.nav')
const show = document.querySelector('.showcase')

function menu(){
    show.classList.toggle('active')
    menuClick.classList.toggle('active')
}